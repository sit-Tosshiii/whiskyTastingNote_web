# database/seeds

このディレクトリには、Postgres へ投入するサンプル SQL や追加の seed スクリプトを配置します。現状は `apps/web/scripts/seedSampleData.js` を使った JSON ベースの投入が中心ですが、将来的に SQL を共有したい場合はここにファイルを追加し、`docs/data_seeding.md` から参照してください。

